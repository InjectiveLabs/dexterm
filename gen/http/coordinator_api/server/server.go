// Code generated by goa v3.1.1, DO NOT EDIT.
//
// CoordinatorAPI HTTP server
//
// Command:
// $ goa gen github.com/InjectiveLabs/injective-core/api/design -o ../

package server

import (
	"context"
	"net/http"

	coordinatorapi "github.com/InjectiveLabs/injective-core/api/gen/coordinator_api"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
	"goa.design/plugins/v3/cors"
)

// Server lists the CoordinatorAPI service endpoint HTTP handlers.
type Server struct {
	Mounts             []*MountPoint
	Configuration      http.Handler
	RequestTransaction http.Handler
	SoftCancels        http.Handler
	CORS               http.Handler
}

// ErrorNamer is an interface implemented by generated error structs that
// exposes the name of the error as defined in the design.
type ErrorNamer interface {
	ErrorName() string
}

// MountPoint holds information about the mounted endpoints.
type MountPoint struct {
	// Method is the name of the service method served by the mounted HTTP handler.
	Method string
	// Verb is the HTTP method used to match requests to the mounted handler.
	Verb string
	// Pattern is the HTTP request path pattern used to match requests to the
	// mounted handler.
	Pattern string
}

// New instantiates HTTP handlers for all the CoordinatorAPI service endpoints
// using the provided encoder and decoder. The handlers are mounted on the
// given mux using the HTTP verb and path defined in the design. errhandler is
// called whenever a response fails to be encoded. formatter is used to format
// errors returned by the service methods prior to encoding. Both errhandler
// and formatter are optional and can be nil.
func New(
	e *coordinatorapi.Endpoints,
	mux goahttp.Muxer,
	decoder func(*http.Request) goahttp.Decoder,
	encoder func(context.Context, http.ResponseWriter) goahttp.Encoder,
	errhandler func(context.Context, http.ResponseWriter, error),
	formatter func(err error) goahttp.Statuser,
) *Server {
	return &Server{
		Mounts: []*MountPoint{
			{"Configuration", "GET", "/api/coordinator/v2/configuration"},
			{"RequestTransaction", "POST", "/api/coordinator/v2/request_transaction"},
			{"SoftCancels", "POST", "/api/coordinator/v2/soft_cancels"},
			{"CORS", "OPTIONS", "/api/coordinator/v2/configuration"},
			{"CORS", "OPTIONS", "/api/coordinator/v2/request_transaction"},
			{"CORS", "OPTIONS", "/api/coordinator/v2/soft_cancels"},
		},
		Configuration:      NewConfigurationHandler(e.Configuration, mux, decoder, encoder, errhandler, formatter),
		RequestTransaction: NewRequestTransactionHandler(e.RequestTransaction, mux, decoder, encoder, errhandler, formatter),
		SoftCancels:        NewSoftCancelsHandler(e.SoftCancels, mux, decoder, encoder, errhandler, formatter),
		CORS:               NewCORSHandler(),
	}
}

// Service returns the name of the service served.
func (s *Server) Service() string { return "CoordinatorAPI" }

// Use wraps the server handlers with the given middleware.
func (s *Server) Use(m func(http.Handler) http.Handler) {
	s.Configuration = m(s.Configuration)
	s.RequestTransaction = m(s.RequestTransaction)
	s.SoftCancels = m(s.SoftCancels)
	s.CORS = m(s.CORS)
}

// Mount configures the mux to serve the CoordinatorAPI endpoints.
func Mount(mux goahttp.Muxer, h *Server) {
	MountConfigurationHandler(mux, h.Configuration)
	MountRequestTransactionHandler(mux, h.RequestTransaction)
	MountSoftCancelsHandler(mux, h.SoftCancels)
	MountCORSHandler(mux, h.CORS)
}

// MountConfigurationHandler configures the mux to serve the "CoordinatorAPI"
// service "configuration" endpoint.
func MountConfigurationHandler(mux goahttp.Muxer, h http.Handler) {
	f, ok := handleCoordinatorAPIOrigin(h).(http.HandlerFunc)
	if !ok {
		f = func(w http.ResponseWriter, r *http.Request) {
			h.ServeHTTP(w, r)
		}
	}
	mux.Handle("GET", "/api/coordinator/v2/configuration", f)
}

// NewConfigurationHandler creates a HTTP handler which loads the HTTP request
// and calls the "CoordinatorAPI" service "configuration" endpoint.
func NewConfigurationHandler(
	endpoint goa.Endpoint,
	mux goahttp.Muxer,
	decoder func(*http.Request) goahttp.Decoder,
	encoder func(context.Context, http.ResponseWriter) goahttp.Encoder,
	errhandler func(context.Context, http.ResponseWriter, error),
	formatter func(err error) goahttp.Statuser,
) http.Handler {
	var (
		decodeRequest  = DecodeConfigurationRequest(mux, decoder)
		encodeResponse = EncodeConfigurationResponse(encoder)
		encodeError    = EncodeConfigurationError(encoder, formatter)
	)
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		ctx := context.WithValue(r.Context(), goahttp.AcceptTypeKey, r.Header.Get("Accept"))
		ctx = context.WithValue(ctx, goa.MethodKey, "configuration")
		ctx = context.WithValue(ctx, goa.ServiceKey, "CoordinatorAPI")
		payload, err := decodeRequest(r)
		if err != nil {
			if err := encodeError(ctx, w, err); err != nil {
				errhandler(ctx, w, err)
			}
			return
		}
		res, err := endpoint(ctx, payload)
		if err != nil {
			if err := encodeError(ctx, w, err); err != nil {
				errhandler(ctx, w, err)
			}
			return
		}
		if err := encodeResponse(ctx, w, res); err != nil {
			errhandler(ctx, w, err)
		}
	})
}

// MountRequestTransactionHandler configures the mux to serve the
// "CoordinatorAPI" service "request_transaction" endpoint.
func MountRequestTransactionHandler(mux goahttp.Muxer, h http.Handler) {
	f, ok := handleCoordinatorAPIOrigin(h).(http.HandlerFunc)
	if !ok {
		f = func(w http.ResponseWriter, r *http.Request) {
			h.ServeHTTP(w, r)
		}
	}
	mux.Handle("POST", "/api/coordinator/v2/request_transaction", f)
}

// NewRequestTransactionHandler creates a HTTP handler which loads the HTTP
// request and calls the "CoordinatorAPI" service "request_transaction"
// endpoint.
func NewRequestTransactionHandler(
	endpoint goa.Endpoint,
	mux goahttp.Muxer,
	decoder func(*http.Request) goahttp.Decoder,
	encoder func(context.Context, http.ResponseWriter) goahttp.Encoder,
	errhandler func(context.Context, http.ResponseWriter, error),
	formatter func(err error) goahttp.Statuser,
) http.Handler {
	var (
		decodeRequest  = DecodeRequestTransactionRequest(mux, decoder)
		encodeResponse = EncodeRequestTransactionResponse(encoder)
		encodeError    = EncodeRequestTransactionError(encoder, formatter)
	)
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		ctx := context.WithValue(r.Context(), goahttp.AcceptTypeKey, r.Header.Get("Accept"))
		ctx = context.WithValue(ctx, goa.MethodKey, "request_transaction")
		ctx = context.WithValue(ctx, goa.ServiceKey, "CoordinatorAPI")
		payload, err := decodeRequest(r)
		if err != nil {
			if err := encodeError(ctx, w, err); err != nil {
				errhandler(ctx, w, err)
			}
			return
		}
		res, err := endpoint(ctx, payload)
		if err != nil {
			if err := encodeError(ctx, w, err); err != nil {
				errhandler(ctx, w, err)
			}
			return
		}
		if err := encodeResponse(ctx, w, res); err != nil {
			errhandler(ctx, w, err)
		}
	})
}

// MountSoftCancelsHandler configures the mux to serve the "CoordinatorAPI"
// service "soft_cancels" endpoint.
func MountSoftCancelsHandler(mux goahttp.Muxer, h http.Handler) {
	f, ok := handleCoordinatorAPIOrigin(h).(http.HandlerFunc)
	if !ok {
		f = func(w http.ResponseWriter, r *http.Request) {
			h.ServeHTTP(w, r)
		}
	}
	mux.Handle("POST", "/api/coordinator/v2/soft_cancels", f)
}

// NewSoftCancelsHandler creates a HTTP handler which loads the HTTP request
// and calls the "CoordinatorAPI" service "soft_cancels" endpoint.
func NewSoftCancelsHandler(
	endpoint goa.Endpoint,
	mux goahttp.Muxer,
	decoder func(*http.Request) goahttp.Decoder,
	encoder func(context.Context, http.ResponseWriter) goahttp.Encoder,
	errhandler func(context.Context, http.ResponseWriter, error),
	formatter func(err error) goahttp.Statuser,
) http.Handler {
	var (
		decodeRequest  = DecodeSoftCancelsRequest(mux, decoder)
		encodeResponse = EncodeSoftCancelsResponse(encoder)
		encodeError    = EncodeSoftCancelsError(encoder, formatter)
	)
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		ctx := context.WithValue(r.Context(), goahttp.AcceptTypeKey, r.Header.Get("Accept"))
		ctx = context.WithValue(ctx, goa.MethodKey, "soft_cancels")
		ctx = context.WithValue(ctx, goa.ServiceKey, "CoordinatorAPI")
		payload, err := decodeRequest(r)
		if err != nil {
			if err := encodeError(ctx, w, err); err != nil {
				errhandler(ctx, w, err)
			}
			return
		}
		res, err := endpoint(ctx, payload)
		if err != nil {
			if err := encodeError(ctx, w, err); err != nil {
				errhandler(ctx, w, err)
			}
			return
		}
		if err := encodeResponse(ctx, w, res); err != nil {
			errhandler(ctx, w, err)
		}
	})
}

// MountCORSHandler configures the mux to serve the CORS endpoints for the
// service CoordinatorAPI.
func MountCORSHandler(mux goahttp.Muxer, h http.Handler) {
	h = handleCoordinatorAPIOrigin(h)
	f, ok := h.(http.HandlerFunc)
	if !ok {
		f = func(w http.ResponseWriter, r *http.Request) {
			h.ServeHTTP(w, r)
		}
	}
	mux.Handle("OPTIONS", "/api/coordinator/v2/configuration", f)
	mux.Handle("OPTIONS", "/api/coordinator/v2/request_transaction", f)
	mux.Handle("OPTIONS", "/api/coordinator/v2/soft_cancels", f)
}

// NewCORSHandler creates a HTTP handler which returns a simple 200 response.
func NewCORSHandler() http.Handler {
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		w.WriteHeader(200)
	})
}

// handleCoordinatorAPIOrigin applies the CORS response headers corresponding
// to the origin for the service CoordinatorAPI.
func handleCoordinatorAPIOrigin(h http.Handler) http.Handler {
	origHndlr := h.(http.HandlerFunc)
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		origin := r.Header.Get("Origin")
		if origin == "" {
			// Not a CORS request
			origHndlr(w, r)
			return
		}
		if cors.MatchOrigin(origin, "*") {
			w.Header().Set("Access-Control-Allow-Origin", origin)
			w.Header().Set("Access-Control-Allow-Credentials", "false")
			if acrm := r.Header.Get("Access-Control-Request-Method"); acrm != "" {
				// We are handling a preflight request
				w.Header().Set("Access-Control-Allow-Methods", "GET, POST")
				w.Header().Set("Access-Control-Allow-Headers", "Content-Type")
			}
			origHndlr(w, r)
			return
		}
		origHndlr(w, r)
		return
	})
}
